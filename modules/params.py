# ----------------------------------------
# Define paths (relative to code)
path_base = '../'

# Source data, designs, and meta-data
path_inputs = path_base + 'inputs/'
path_rawdata = path_inputs + 'bris/'
path_pp = path_rawdata + 'pp/'
path_res = path_rawdata + 'res/'

# Output data
path_outputs = path_base + 'outputs/'
path_log = path_outputs + 'log/'
path_tmp = path_outputs + 'tmp/'
path_features = path_outputs + 'features/'

# ----------------------------------------
# Feature processing parameters
max_npp = 10
max_nwrk = 12
